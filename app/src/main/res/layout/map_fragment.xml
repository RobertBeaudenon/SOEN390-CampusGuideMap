<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:map="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/coordinate_layout"
    tools:context=".views.MapFragment">

    <fragment
        android:id="@+id/map"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:name="com.google.android.gms.maps.SupportMapFragment" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_search"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="112dp"
        android:layout_marginEnd="8dp"
        app:srcCompat="@android:drawable/ic_menu_search"
        android:tint="@color/quantum_black_100"
        app:backgroundTint="@color/quantum_white_100"
        app:fabSize="normal"
        app:layout_dodgeInsetEdges="bottom"
        />

    <LinearLayout
        android:id="@+id/toggle_button"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_gravity="left"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:layout_marginTop="80dp"
        android:background="#fff"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/text_SGW"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/SGW_Campus_Name"
            android:layout_gravity="center"/>

        <Switch
            android:id="@+id/toggle_Campus"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:checked="false"
            android:showText="false"
            android:splitTrack="false"
            android:textOff="@string/Loyola_Campus_Name"
            android:textOn="@string/SGW_Campus_Name"
            tools:checked="false" />

        <TextView
            android:id="@+id/text_Loyola"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/Loyola_Campus_Name"
            android:layout_gravity="center"/>

    </LinearLayout>

    <include layout="@layout/bottom_sheet_layout"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>